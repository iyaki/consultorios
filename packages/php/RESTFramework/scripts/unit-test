#!/usr/bin/env bash

CODE_DIRECOTRY='src/'

SCRIPT_DIR="$(dirname $(realpath "$0"))"

"$SCRIPT_DIR/setup"

php -c "${SCRIPT_DIR}/../../../../deploy/dev/php/conf.d/php.ini" "${SCRIPT_DIR}/../vendor/bin/phpunit" "$CODE_DIRECOTRY"
