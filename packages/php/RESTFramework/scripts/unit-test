#!/usr/bin/env bash

CODE_DIRECOTRY='src/'

SCRIPT_DIR="$(dirname $(realpath "$0"))"

"$SCRIPT_DIR/setup"

"${SCRIPT_DIR}/../vendor/bin/phpunit" \
    --coverage-text \
    --prepend="/root/.composer/vendor/autoload.php" \
    --do-not-cache-result
